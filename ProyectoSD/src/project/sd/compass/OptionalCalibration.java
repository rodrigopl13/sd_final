package project.sd.compass;

public class OptionalCalibration {
	
	public static void main() {
		//star calibrate
				/*compassSensor.startCalibration();

				//try to make two full turns to calibrate the compass
				while (totalDegrees < 1000) {
					 
					//hacer arcos de 18 grados por segundo
					pilot.arc(10, degrees);
					
					time += segs;
					totalDegrees += degrees;
					
					System.out.println("\n time: " + time);
					System.out.println("D: " + degrees + " tD: " + totalDegrees);
					
					Delay.msDelay(segs);
				} 
				
				
				//stop calibration
				compassSensor.stopCalibration();
				System.out.println("\n\n\n stopCalibration: ");Delay.msDelay(3000);
				
				Delay.msDelay(5000);
				System.out.println("\npose: " + pose.getLocation());Delay.msDelay(5000);
				
				Point point = pose.getLocation();
				System.out.println("stringP: " + point.toString());Delay.msDelay(5000);*/
	}

}
